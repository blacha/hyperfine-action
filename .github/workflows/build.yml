name: Main
on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: linz/action-typescript@v2

    - name: Bundle
      run: yarn run bundle

    - name: Test Action
      uses: ./

