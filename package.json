{
  "name": "hyperfine-action",
  "version": "0.0.1",
  "main": "build/index.js",
  "scripts": {
    "build": "tsc --pretty",
    "lint": "eslint 'src/**/*.ts' --quiet --fix",
    "test": "node --test",
    "bundle": "npx esbuild src/action.ts --bundle --outdir=dist/ --platform=node"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@actions/core": "^1.10.0",
    "@actions/github": "^5.1.1",
    "@linzjs/style": "^3.15.0",
    "@types/node": "^18.13.0",
    "@types/ospec": "^4.0.5",
    "esbuild": "^0.17.6"
  },
  "dependencies": {}
}
